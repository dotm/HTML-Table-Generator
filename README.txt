The table generator code is in src directory.
The spec contains test using jasmine.
The lib contains jasmine and jQuery.

Use index to try the generator.
SpecRunner is used to run tests
script.js is used to setup index.html.